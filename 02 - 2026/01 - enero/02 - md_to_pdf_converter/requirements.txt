# Dependencias para conversión Markdown a PDF
# Instalar todas: pip install -r requirements.txt
# O instalar solo las necesarias según el método que quieras usar

# Método recomendado (WeasyPrint)
markdown>=3.4.0
weasyprint>=59.0

# Método Pandoc (requiere también instalar pandoc en el sistema)
pypandoc>=1.11

# Método simple (md2pdf)
md2pdf>=0.1.0

# Método ReportLab
reportlab>=4.0.0
beautifulsoup4>=4.12.0

# Opcional: para colores en la terminal
colorama>=0.4.6

